# 本地私有 / 运行时生成，不上传 GitHub

# 本地工具目录：push.py 含 SSH 凭据，其余为历史构建残留
_ui/

# 账号数据（密码哈希）与接口缓存，首次启动自动生成
users.json
cache.json
monitor.json
notifications.json

# 系统与临时文件
.DS_Store
__pycache__/
*.pyc
nohup.out
*.log
.venv/

# 本地双击启动器（不上传，仅本地使用）
启动 (macOS).command
启动 (Windows).bat

# 本地测试脚本不入库
tests/
.pytest_cache/
